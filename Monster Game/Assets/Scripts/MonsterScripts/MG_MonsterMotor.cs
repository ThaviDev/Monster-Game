using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MG_MonsterMotor : MonoBehaviour {
    /*
	public GameObject _player;// El objeto del jugador en la escena
    public Transform lastPlayerPos;// La ultima posicion donde el monstruo perseguidor vió al jugador
    public Transform chaserWonderingPos;// Las posiciones alrededor del jugador a donde se dirige el monstruo perseguidor cuando esta en estado de busqueda
    public GameObject rockDestroyerGMO;// El objeto con la colision para destruir la roca del monstruo perseguidor
    //bool isChasingPlayer = false;
    float timeOnRock; // Medida de tiempo que el monstruo perseguidor utiliza para romper la roca
    float patienceTimeFT; // Medida de tiempo utilizada de varias maneras por el monstruo perseguidor
    bool playerDetection = false; // Si se detectó al jugador en el area de vision del monstruo perseguidor
    //public AudioSource monsterAudioSource;
    //public AudioClip chasingSound;
    //public AudioClip wonderingSound;
    bool chasePlayerConstant = true; // Si se detectó al jugador en el area de vision del monstruo perseguidor
    float wonderingDirection = 1f;
    bool hasDesicionTaken = false;
    public Animator anim; // el animador del monstruo
    public GameObject monstVisual; // Visual del monstruo en pantalla
    public Vector3 lastPos; //la ultima posicion de vector 2 del monstruo (esta variable sirve para detectar si el monstruo se esta moviendo y hacia que direccion)
    string dirAnim; // detecta a que dirección se mueve el objeto
    float animFloatX = 0; // guarda la informacion de la diferencia entre pos_actual y pos_ultima en x
    float animFloatY = 0; // guarda la informacion de la diferencia entre pos_actual y pos_ultima en y
    private float animTimer; // el timer de la direccion de animacion del monstruo (cuantas veces identifica la direccion de la animacion)
    private float animTimerMax = .1f; // el maximo tiempo del timer de la animacion del monstruo (animTimer)
    */
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
